\lecture{3}{24. March 2026t}{Feedback Control System Characteristics}

\begin{problemwithimage}{E4.11}{E4.11}
  Consider the closed-loop system in the figure, where
  \begin{equation*}
    G(s) = \frac{K}{s + 10}, \qquad H(s) = \frac{14}{s^2 + 5s + 6}
  .\end{equation*}
  \begin{enumerate}[label=(\alph*)]
    \item Compute the transfer function $T(s) = Y(s) / R(s)$.
    \item Define the tracking error to be $E(s) = R(s) - Y(s)$.
      Compute $E(s)$ and determine the steady-state tracking error due to a unit step input, that is, let $R(s) = 1 / s$.
    \item Compute the transfer function $Y(s) / T_d(s)$ and determine the steady-state error of the output due to a unit step disturbance input, that is, let $T_{d}(s) = 1 / s$.
    \item Compute the sensitivity $S_k^{T}$
  \end{enumerate}
\end{problemwithimage}
